apiVersion: apps/v1
kind: Deployment
metadata:
  name: myntra-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myntra
  template:
    metadata:
      labels:
        app: myntra
    spec:
      containers:
      - name: myntra
        image: satyasaia99/myntra:v1.6
        ports:
        - containerPort: 8080
        imagePullPolicy: Always
