apiVersion: v1
kind: Service
metadata:
  name: myntra-service
spec:
  type: NodePort            # Change to LoadBalancer if in cloud
  selector:
    app: myntra
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30004
