apiVersion: v1
kind: Service
metadata:
  name: myntra-service
spec:
  type: LoadBalancer          
  selector:
    app: myntra
  ports:
    - port: 80               
      targetPort: 8080       
      
